# --- Database Credentials (Postgres) ---
POSTGRES_DB=rules_db
POSTGRES_USER=admin
POSTGRES_PASSWORD=admin

# --- Database Credentials (MongoDB) ---
MONGO_INITDB_ROOT_USERNAME=admin
MONGO_INITDB_ROOT_PASSWORD=admin
MONGO_HOST=mongodb
MONGO_PORT=27017
MONGO_DATABASE=enrichment_db
MONGO_COLLECTION=enrichment_rules

# --- Redis Credentials ---
REDIS_HOST=redis
REDIS_PORT=6379

# --- Kafka Brokers ---
KAFKA_BOOTSTRAP_SERVERS=kafka:9092

# --- PGAdmin Credentials ---
PGADMIN_DEFAULT_EMAIL=admin@example.com
PGADMIN_DEFAULT_PASSWORD=admin

# --- Application Ports ---
MANAGEMENT_SERVER_PORT=8081
FILTERING_SERVER_PORT=8082
DEDUPLICATION_SERVER_PORT=8083
BFF_SERVER_PORT=8084
ENRICHMENT_SERVER_PORT=8085

# --- Internal Service Hosts/Ports (based on service names) ---
MANAGEMENT_HOST=java-service-management
MANAGEMENT_PORT=8081

# --- Other common variables ---
KAFKA_CONSUMER_GROUP_ID=group_consumer
KAFKA_CONSUMER_AUTO_OFFSET_RESET=earliest
APPLICATION_UPDATE_INTERVAL_SEC=30
SPRING_PROFILES_ACTIVE=prod

# --- Database URLs for applications ---
DB_JDBC_URL=jdbc:postgresql://${POSTGRES_HOST:-postgres}:${POSTGRES_PORT:-5432}/${POSTGRES_DB}
DB_USER=${POSTGRES_USER}
DB_PASSWORD=${POSTGRES_PASSWORD}
DB_DRIVER=org.postgresql.Driver

# Переменные для Spring Boot DataSource
SPRING_DATASOURCE_URL=${DB_JDBC_URL}
SPRING_DATASOURCE_USERNAME=${DB_USER}
SPRING_DATASOURCE_PASSWORD=${DB_PASSWORD}
SPRING_DATASOURCE_DRIVER_CLASS_NAME=${DB_DRIVER}

# --- Kafka Topic Names ---
TOPIC_1=topic_1
TOPIC_2=topic_2
TOPIC_3=topic_3
TOPIC_4=topic_4

KAFKA_CONSUMER_TOPICS_FILTERING=${TOPIC_1}
KAFKA_PRODUCER_TOPIC_FILTERING=${TOPIC_2}

KAFKA_CONSUMER_TOPICS_DEDUPLICATION=${TOPIC_2}
KAFKA_PRODUCER_TOPIC_DEDUPLICATION=${TOPIC_3}

KAFKA_CONSUMER_TOPICS_ENRICHMENT=${TOPIC_3}
KAFKA_PRODUCER_TOPIC_ENRICHMENT=${TOPIC_4}

BFF_TOPIC_IN=${TOPIC_1}
BFF_TOPIC_OUT=${TOPIC_4}